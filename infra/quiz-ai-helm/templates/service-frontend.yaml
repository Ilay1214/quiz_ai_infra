apiVersion: v1
kind: Service
metadata:
  name: {{ include "quiz-ai-helm.fullname" . }}-frontend
  labels:
    {{- include "quiz-ai-helm.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
  {{- with .Values.frontend.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.frontend.service.type | default "ClusterIP" }}
  selector:
    {{- include "quiz-ai-helm.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: {{ .Values.frontend.service.port | default 80 }}
      targetPort: {{ .Values.frontend.service.targetPort | default "http" }}
